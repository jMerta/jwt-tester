# jwt-tester-tool (npm)

Install globally:

```bash
npm install -g jwt-tester-tool
jwt-tester --help
```

This is a meta-package that installs a platform-specific binary from
`@jakmer/*` packages. It is not meant to be installed directly by name.

Repository: https://github.com/jMerta/jwt-tester
